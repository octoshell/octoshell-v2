<%= form_with(model: bot_link, local: true) do |form| %>
  <% if bot_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bot_link.errors.count, "error") %> prohibited this bot_link from being saved:</h2>

      <ul>
      <% bot_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :token %>
    <%= form.text_field :token %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
