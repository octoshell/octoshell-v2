= render "header"

p #{t('.msg_report_picked', id: @report.id, pid: @report.project.id, title: @report.project.title, session: @report.session.description)}
p #{t('.msg_follow_comments')}

= render "footer"
