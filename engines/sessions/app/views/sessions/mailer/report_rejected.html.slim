= render "header"

p #{t('.msg_report_rejected', id: @report.id, pid: @report.project.id, title: @report.project.title, session: @report.session.description)}
p #{t('.msg_report_update')}
p = link_to t('.proceed_to_report'), sessions.report_url(@report)

= render "footer"
