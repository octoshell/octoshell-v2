= render "header"

p #{t('.msg_survey_postdated', survey_name: @user_survey.survey.name, session: @user_survey.session.description)}
h5 #{t('.msg_account_blocked')}

p = link_to t('.proceed_to_survey'), sessions.user_survey_url(@user_survey)

= render "footer"
