= render "header"

p #{t('.msg_report_ready', id: @report.id, pid: @report.project.id, title: @report.project.title, session: @report.session.description)}
p = link_to t('.assess_report'), sessions.admin_report_url(@report)

= render "mailer/footer"
