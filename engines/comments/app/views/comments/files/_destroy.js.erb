
main_element.on("click",'.delete-button',onDestroy);
function onDestroy()
{
	main_element.find('#table').find('button').attrProp('disabled',true);
	var id = $(this).parents("article").attr('file-id');
	$.ajax({
	  url: `/comments/files/${id}`,
		method: "DELETE"
	}).done(function() {
		pageClick();
	});
}
