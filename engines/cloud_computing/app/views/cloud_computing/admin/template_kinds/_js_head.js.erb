$(document).ready(function(){
  setup_template_kind_tree();
  $('button#create_root').click(function () {
    $("#template_kinds_tree").jstree("create_node", null, null, "last", function (node) {
      this.edit(node);
    });
  });
});
