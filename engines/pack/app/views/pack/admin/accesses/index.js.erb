

     $(document).on('change','#q_who_type_in',function()
    {
     
      if ($(this).find('option:selected').length == 1)
      {
        
        var temp= $(this).find('option:selected');
        
      }
    });

